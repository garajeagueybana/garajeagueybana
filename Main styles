/* Reset */
* { margin: 0; padding: 0; box-sizing: border-box; }

html {
  background: url("https://raw.githubusercontent.com/RubenCentenoRoman/Website---Agueybana/192dfcb2c3a48b4789e13a05bc90c75cfa941094/Images/Default_BG.webp") center/cover no-repeat;
  height: 100%;
  scroll-padding-top: 10rem;
}

:root{
  --brand: rgb(202,169,61);
  --red: #c80e0e;
  --red-dark: #8f0a0a;
  --white: #fff;
  --black: #000;
}

@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; scroll-behavior: auto !important; }
}

:focus-visible { outline: 2px solid var(--brand); outline-offset: 2px; }
img { max-width: 100%; height: auto; display: block; }

body { font-family: system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial,"Noto Sans",sans-serif; line-height: 1.6; }

/* Top Navigation */
.top-nav {
  display:flex; flex-wrap:wrap; justify-content:space-between; align-items:center;
  padding:1rem; min-height:10rem; background:black; color:white; gap:1rem;
  position:fixed; top:0; left:0; right:0; z-index:1000;
}

/* Hamburger */
.hamburger { display:none; flex-direction:column; cursor:pointer; padding:.5rem; z-index:1001; background:none; border:0; }
.hamburger span { width:30px; height:3px; background:white; margin:4px 0; transition:.3s; border-radius:2px; }
.hamburger.active span:nth-child(1){ transform:rotate(-45deg) translate(-6px, 6px); }
.hamburger.active span:nth-child(2){ opacity:0; }
.hamburger.active span:nth-child(3){ transform:rotate(45deg) translate(-6px, -6px); }

.logo { width: clamp(10rem, 20vw, 18rem); height:auto; }

/* Nav center como links */
.nav-center a {
  display:inline-block; background:var(--brand); color:white; padding:.8rem 5rem; border-radius:5px; font-size:1rem;
  cursor:pointer; position:relative; overflow:hidden; text-decoration:none;
}
.nav-center a::after {
  content:' \00bb'; position:absolute; opacity:0; top:50%; transform:translateY(-50%); right:1rem; transition:.5s; pointer-events:none;
}
.nav-center a:hover { padding-right:6rem; }
.nav-center a:hover::after { opacity:1; right:1.5rem; }
.nav-center a[aria-current="page"] { text-decoration: underline; }

/* Right contacts */
.nav-right { display:flex; flex-direction:column; gap:.5rem; }
.contact { display:flex; align-items:center; gap:.5rem; background:white; color:black; padding:.5rem; border-radius:5px; }
.contact img { width:24px; height:24px; }
.contact.phone { background:#32CD32; }
.contact.instagram { background:#f71010; color:white; }

/* Heading */
.heading {
  background:url("https://raw.githubusercontent.com/RubenCentenoRoman/Website---Agueybana/192dfcb2c3a48b4789e13a05bc90c75cfa941094/Images/Top_BG_3.webp") center/cover no-repeat;
  min-height:45vh; display:flex; justify-content:center; align-items:center; text-align:center; color:white; padding:1rem; margin-top:10rem;
}
.heading-content { display:flex; flex-direction:column; align-items:center; gap:2rem; }
.heading h1, .heading p { background:rgba(0,0,0,.5); padding:.5rem 1rem; border-radius:5px; }
.heading h1 { font-size: clamp(1.5rem, 4vw, 2.5rem); }
.heading p { font-size: clamp(1rem, 2.5vw, 1.5rem); }
.login-btn { background:var(--red); color:white; padding:.8rem 2rem; border:none; border-radius:5px; font-size:1rem; cursor:pointer; transition:background .3s; }
.login-btn:hover { background:var(--red-dark); }

/* Mobile Navigation */
@media (max-width: 768px) {
  .top-nav { min-height:auto; padding:1rem; }
  .hamburger { display:flex; order:1; }
  .nav-left { order:2; flex:1; text-align:center; }
  .logo { width: clamp(8rem, 30vw, 12rem); }
  .nav-center {
    order:4; position:fixed; top:0; left:-100%; width:80%; max-width:300px; height:100vh;
    background:rgba(0,0,0,.98); display:flex; flex-direction:column; gap:.5rem; padding:5rem 1rem 1rem; transition:left .3s; z-index:999;
  }
  .nav-center.active { left:0; }
  .nav-right { order:3; flex-direction:column; gap:.3rem; }
  .contact { font-size:.75rem; padding:.3rem; }
  .contact img { width:20px; height:20px; }
  .heading { margin-top:5rem; padding-top:2rem; }
}

/* Summary Services (index) */
.summary-services {
  display:flex; min-height:85vh;
  border-top:white solid .5vh; border-bottom:black solid .5vh;
  background:linear-gradient(rgba(0,0,0,.5), rgba(0,0,0,.5)), url("https://raw.githubusercontent.com/RubenCentenoRoman/Website---Agueybana/192dfcb2c3a48b4789e13a05bc90c75cfa941094/Images/Speedometer_Sum.webp") center/cover no-repeat;
}
.interact-summary, .summary-text { flex:1; display:flex; flex-direction:column; justify-content:center; align-items:center; padding:1rem; }
.img-box { border:2px solid var(--brand); width: clamp(250px, 50vh, 600px); height: clamp(150px, 30vh, 400px); overflow:hidden; }
.img-box img { width:100%; height:100%; object-fit:cover; }
.nav { margin-top:10px; font-size:1.2em; display:flex; gap:10px; align-items:center; color:white; }
.nav button { cursor:pointer; background:none; border:1px solid var(--brand); color:var(--brand); padding:.3rem .6rem; border-radius:4px; }
.summary-text { background:#262626; color:white; padding:20px; text-align:center; display:flex; flex-direction:column; align-items:center; justify-content:center; }
.image-placeholder { border:1px solid black; width:80%; max-width:500px; height:200px; display:flex; align-items:center; justify-content:center; overflow:hidden; }
.image-placeholder img { width:100%; height:100%; object-fit:cover; }
.summary-text h2 { margin:15px 0; font-size: clamp(1.2rem, 3vw, 1.8rem); }
.summary-text p { font-size: clamp(.9rem, 2vh, 1.2rem); text-align: justify; margin:20px 0; padding: 0 clamp(1rem, 5vw, 20vh); padding-bottom: clamp(2rem, 10vh, 20vh); line-height:1.5em; }
.summary-text hr { border:none; border-top:2px solid black; width:90%; margin:20px 0; }
.small-title { border:1px solid black; padding:5px; margin-top:10px; font-size: clamp(.9rem, 2vw, 1.2rem); }
@media (max-width: 768px) { .summary-services { flex-direction:column; min-height:auto; } .summary-text p { padding:0 1rem; padding-bottom:2rem; text-align:start; } }

/* Animations */
@keyframes slideInRight { 0%{transform:translateX(100%); opacity:0;} 100%{transform:translateX(0); opacity:1;} }
.animate-text { animation: slideInRight .5s ease forwards; }

/* Subscription Cards (index) */
.subscription {
  display:flex; align-items:stretch; justify-content:center; gap:5vh;
  background:url("https://raw.githubusercontent.com/RubenCentenoRoman/Website---Agueybana/192dfcb2c3a48b4789e13a05bc90c75cfa941094/Images/Car_Brand_BG_2.webp"); background-size:cover; background-repeat:repeat; background-position:center;
  width:100%; padding:3rem 1rem; border-bottom:1vh solid black;
}
.subscription article {
  flex:1; max-width:400px; display:flex; flex-direction:column; position:relative; color:white; background:black;
  padding-top:1vh; text-align:center; min-height:600px; border-radius:2vh; box-shadow:.1rem 1rem 3rem black; border:.3rem solid black;
  width:25%; max-width:400px; overflow:hidden; transition: transform .25s ease, box-shadow .25s ease; cursor:pointer; will-change:transform;
}
.subscription article:hover { transform: scale(1.03); box-shadow: 0 10px 30px rgba(0,0,0,.35); }
.subscription article img { width:80%; max-width:200px; height:auto; margin:1rem auto; }
.subscription article h3 { font-size: clamp(1.3rem, 3vw, 1.8rem); margin:.5rem 0; }
.subscription article small { display:block; font-size: clamp(.8rem, 2vw, .9rem); color:#a4a3a3; margin-bottom:1rem; }
.subscription article h4 { margin:.2rem 0; }
.price { font-weight:bold; font-size: clamp(1.2rem, 3vw, 1.4rem); }
.renewal { font-size: clamp(.9rem, 2vw, 1rem); font-weight:normal; margin-left:.3rem; }
.subscription article ul { text-align:left; padding-left:1.2rem; margin-top:1.5rem; font-size: clamp(.85rem, 2vw, .95rem); }
.subscription article li { list-style-position: inside; margin-bottom:.5rem; padding-right:1rem; }
@media screen and (max-width:960px){ .subscription{ flex-direction:column; align-items:center; gap:3vh;} .subscription article{ width:90%; max-width:500px; } }

/* Advertisement (index) */
.advertisement { font-weight:bold; font-size:1.2rem; background:black; color:white; text-align:center; padding:1rem; display:flex; flex-direction:column; align-items:center; gap:1rem; }
.advertisement button { background:var(--red); color:white; border:none; padding:.6rem 1rem; font-weight:bold; font-size:1.2rem; border-radius:5px; cursor:pointer; width:100%; }
.advertisement button:hover { background:var(--red-dark); }
.advertisement-anchor { text-decoration:none; }
@media (min-width:480px){ .advertisement{ flex-direction:row; justify-content:center; } .advertisement button{ width:auto; } }

/* Services grid (subscripcion) */
.services { display:grid; grid-template-columns: repeat(auto-fit, minmax(250px,1fr)); background-color:#3f3f3f; color:white; padding:2rem 1rem 6rem; gap:2rem; text-align:center; border-bottom:1vh solid black; }
.services-title { border-top:black solid 1vh; padding:1.5rem 1rem; background:#3f3f3f; text-shadow:2px 2px 2px black; color:orange; width:100%; text-align:center; font-size: clamp(1.3rem, 3vw, 1.8rem); }
.service-box { display:flex; flex-direction:column; align-items:center; gap:1rem; max-width:300px; margin:0 auto; padding:0 1rem; opacity:0; transform:translateY(30px); animation: slideInTop .8s ease-out forwards; }
.service-box:nth-child(1){ animation-delay:.1s;} .service-box:nth-child(2){ animation-delay:.2s;} .service-box:nth-child(3){ animation-delay:.3s;} .service-box:nth-child(4){ animation-delay:.4s;}
.service-box img { width: clamp(50px,10vw,60px); height: clamp(50px,10vw,60px); }
.service-box h3 { color:orange; font-size: clamp(1rem, 2.5vw, 1.2rem); font-weight:bold; margin-bottom:.5rem; }
.service-box p { font-size: clamp(.85rem, 2vw, .95rem); line-height:1.4; color:#dcdcdc; }
@keyframes slideInTop { to { opacity:1; transform: translateY(0);} }
@media (max-width:768px){ .services{ grid-template-columns:1fr; padding:2rem .5rem;} .service-box{ max-width:100%; } }

/* Vehicle Form (subscripcion) */
main { background:black; }
.vehicle-form { background:#2a2a2a; color:white; padding:2rem 1.5rem; max-width:600px; margin:2rem auto; border-radius:12px; box-shadow:0 6px 12px rgba(0,0,0,.3); }
.form-group { margin-bottom:1.5rem; }
.form-group label { color:orange; font-weight:bold; display:block; margin-bottom:.5rem; font-size: clamp(.9rem, 2vw, 1rem); }
.form-group input, .form-group select { width:100%; padding:.7rem; margin-top:.3rem; border-radius:6px; border:1px solid #555; font-size: clamp(.9rem, 2vw, 1rem); background:#3a3a3a; color:white; outline:none; }
.form-group input:focus, .form-group select:focus { border-color:var(--brand); box-shadow:0 0 0 3px rgba(202,169,61,.25); }
.form-group input:invalid, .form-group select:invalid { border-color:#ff9800; }
.select-btn { width:100%; padding:.8rem 1.5rem; background:orange; color:#111; border:none; border-radius:6px; font-size: clamp(1rem, 2.5vw, 1.1rem); font-weight:bold; cursor:pointer; transition:background .3s; margin-top:1rem; }
.select-btn:hover { background:#ff8c00; }
.result-card { margin-top:1.5rem; padding:1.5rem; border-radius:10px; text-align:center; font-size: clamp(1rem, 2.5vw, 1.2rem); transition: transform .2s, box-shadow .2s; }
.result-card:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(0,0,0,.25); }
.result-card.sedan    { border: 2px solid orange;    background: rgba(255,165,0,.08); }
.result-card.suv      { border: 2px solid lightblue; background: rgba(173,216,230,.08); }
.result-card.premium  { border: 2px solid gold;      background: #444; }
.result-card.electric { border: 2px solid #e74c3c;   background: rgba(231,76,60,.08); color: #ff6b6b; }
.result-card.error    { border: 2px solid #ff9800;   background: rgba(255,152,0,.08); color: #ff9800; }
.plan-display { width:100%; padding:.7rem; margin-top:.3rem; border-radius:6px; font-size: clamp(.9rem, 2vw, 1rem); background:#444; color:white; opacity:.9; min-height:45px; display:flex; align-items:center; justify-content:center; }

/* Footer */
footer {
  position: relative;
  background: linear-gradient(rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.2)), url("https://raw.githubusercontent.com/RubenCentenoRoman/Website---Agueybana/192dfcb2c3a48b4789e13a05bc90c75cfa941094/Images/bottom_part_BG_2.webp");
  background-size: cover; background-repeat: repeat; background-position: center;
  width: 100%; min-height: 50vh; color: white; display: flex; justify-content: space-around; align-items: center;
  padding: 2em 1em; box-sizing: border-box; flex-wrap: wrap; gap: 2rem;
}
.footer-box { background-color: rgba(0, 0, 0, 0.5); border: 2px solid white; padding: 1.5em; text-align: center; display: flex; flex-direction: column; align-items: center; gap: 1em; min-width: 250px; }
.FooterSiteName .footer-box { padding: 0; }
.FooterSiteName .logo { width: 100%; height: 12vh; min-height: 80px; object-fit: contain; display: block; }
.FooterSiteSocial h2, .FooterSiteNews h2 { color: yellow; font-size: clamp(1.2rem, 3vw, 1.6rem); font-weight: bold; border: 2px solid white; padding: 0.5em 1em; display: inline-block; margin-bottom: 1em; }
.FollowSocials { display: flex; gap: 1em; justify-content: center; flex-wrap: wrap; }
.FollowSocials a { display: flex; align-items: center; gap: 0.5em; color: white; text-decoration: none; padding: 0.5em 1em; border-radius: 8px; }
.FollowSocials img { width: 30px; height: 30px; }
.SignUp { display: inline-block; background: var(--red); color: white; padding: 0.8em 2em; border: 2px solid white; border-radius: 2em; font-size: clamp(1rem, 3vw, 1.4rem); text-decoration: none; font-weight: bold; transition: background 0.3s; }
.SignUp:hover { background: var(--red-dark); }

@media (max-width: 768px) {
  footer { flex-direction: column; padding: 2em 0.5em; }
  .footer-box { width: 90%; max-width: 400px; }
}
